pub enum IrOpcode
{
    Br, // Terminators
    CondBr,
    Ret,
    Unreachable,
    Noret,

    Neg,  // Unary
    Not,

    Add, // Binary arithmetic
    Sub,
    Mul,
    SDiv,
    UDiv,
    SRem,
    URem,

    And, // Binary bitwise
    Or,
    Xor,
    Shl,
    Ashr,
    Lshr,

    Alloca, // Memory
    Load,
    Store,
    Gep, // Cast

    Trunc,
    Zext,
    Sext,
    FpTrunc,
    FpExt,
    FpToSi,
    SiToFp,
    FpToUi,
    UiToFp,
    Bitcast,

    ICmp, // Compare
    FCmp,

    CtPop, // Bit manipulation
    CtLz,
    CtTz,
    Rotl,
    Rotr,
    Bswap,

    SMin, // Integer min/max
    SMax,
    UMin,
    UMax,

    Abs, // Integer abs

    FAdd, // Floating-point
    FSub,
    FMul,
    FDiv,
    Fma,
    FAbs,
    FSqrt,

    VecAdd, // Vector/SIMD
    VecSub,
    VecMul,
    VecDiv,
    VecFAdd,
    VecFSub,
    VecFMul,
    VecFDiv,
    VecMin,
    VecMax,
    VecBroadcast,
    ShuffleVector,
    ExtractElement,
    InsertElement,

    InlineAsm, // Inline assembly

    AtomicLoad, // Atomics
    AtomicStore,
    AtomicRmw,
    AtomicCmpXchg,
    Fence,

    LandingPad, // Exception handling
    ExtractValue,

    GcRoot, // GC support
    GcWriteBarrier,
    GcSafepoint,

    Call, // Other
    Phi,
    Select
}

pub enum IrFCmpPred
{
    Oeq,
    One,
    Olt,
    Ole,
    Ogt,
    Oge,
    Ueq,
    Une,
    Ult,
    Ule,
    Ugt,
    Uge
}

// Atomic RMW operations
pub enum IrAtomicRmwOp
{
    Xchg,
    Add,
    Sub,
    And,
    Nand,
    Or,
    Xor,
    Max,
    Min,
    UMax,
    UMin
}

// Atomic memory ordering
pub enum IrAtomicOrdering
{
    Relaxed,
    Acquire,
    Release,
    AcqRel,
    SeqCst
}

pub bool irOpcodeIsTerminator(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Br,
        IrOpcode::CondBr,
        IrOpcode::Ret,
        IrOpcode::Unreachable,
        IrOpcode::Noret => { return true; },
        
        else => { return true; },
    },
}

pub bool irOpcodeIsVector(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::VecAdd,
        IrOpcode::VecSub,
        IrOpcode::VecMul,
        IrOpcode::VecDiv,
        IrOpcode::VecFAdd,
        IrOpcode::VecFSub,
        IrOpcode::VecFMul,
        IrOpcode::VecFDiv,
        IrOpcode::VecMin,
        IrOpcode::VecMax,
        IrOpcode::VecBroadcast,
        IrOpcode::ShuffleVector,
        IrOpcode::ExtractElement,
        IrOpcode::InsertElement => { return true; },
        
        else => { return false; } 
    }
}

pub bool irOpcodeIsAtomic(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::AtomicLoad,
        IrOpcode::AtomicStore,
        IrOpcode::AtomicRmw,
        IrOpcode::AtomicCmpXchg,
        IrOpcode::Fence => { return true; },

        else => { return false; }
    }
}

pub bool irOpcodeIsGc(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::GcRoot,
        IrOpcode::GcWriteBarrier,
        IrOpcode::GcSafepoint => { return true; },

        else => { return false; }
    }
}

pub bool irOpcodeIsBinary(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Add,
        IrOpcode::Sub,
        IrOpcode::Mul,
        IrOpcode::SDiv,
        IrOpcode::UDiv,
        IrOpcode::SRem,
        IrOpcode::URem,
        IrOpcode::And,
        IrOpcode::Or,
        IrOpcode::Xor,
        IrOpcode::Shl,
        IrOpcode::Ashr,
        IrOpcode::Lshr,
        IrOpcode::Rotl,
        IrOpcode::Rotr,
        IrOpcode::SMin,
        IrOpcode::SMax,
        IrOpcode::UMin,
        IrOpcode::UMax,
        IrOpcode::VecAdd,
        IrOpcode::VecSub,
        IrOpcode::VecMul,
        IrOpcode::VecDiv,
        IrOpcode::VecFAdd,
        IrOpcode::VecFSub,
        IrOpcode::VecFMul,
        IrOpcode::VecFDiv,
        IrOpcode::VecMin,
        IrOpcode::VecMax => { return true; },

        else => { return false; }
    }
}

pub bool irOpcodeIsCast(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Trunc,
        IrOpcode::Zext,
        IrOpcode::Sext,
        IrOpcode::FpTrunc,
        IrOpcode::FpExt,
        IrOpcode::FpToSi,
        IrOpcode::SiToFp,
        IrOpcode::FpToUi,
        IrOpcode::UiToFp,
        IrOpcode::Bitcast => { return true; },

        else => { return false; }
    }
}

pub bool irOpcodeIsMemory(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Alloca,
        IrOpcode::Load,
        IrOpcode::Store,
        IrOpcode::Gep,
        IrOpcode::AtomicLoad,
        IrOpcode::AtomicStore => { return true; },
    
        else => { return false; }
    }
}

pub bool irOpcodeIsCommutative(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Add,
        IrOpcode::Mul,
        IrOpcode::And,
        IrOpcode::Or,
        IrOpcode::Xor,
        IrOpcode::SMin,
        IrOpcode::SMax,
        IrOpcode::UMin,
        IrOpcode::UMax => { return true; },
    
        return false; }
    }
}

// Opcode string names
static str irOpcodeStrHelper(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Br => { return "br"; },
        IrOpcode::CondBr => { return "cond_br"; },
        IrOpcode::Ret => { return "ret"; },
        IrOpcode::Unreachable => { return "unreachable"; },
        IrOpcode::Noret => { return "noret"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper2(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Neg => { return "neg"; },
        IrOpcode::Not => { return "not"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper3(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Add => { return "add"; },
        IrOpcode::Sub => { return "sub"; },
        IrOpcode::Mul => { return "mul"; },
        IrOpcode::SDiv => { return "sdiv"; },
        IrOpcode::UDiv => { return "udiv"; },
        IrOpcode::SRem => { return "srem"; },
        IrOpcode::URem => { return "urem"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper4(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::And => { return "and"; },
        IrOpcode::Or => { return "or"; },
        IrOpcode::Xor => { return "xor"; },
        IrOpcode::Shl => { return "shl"; },
        IrOpcode::Ashr => { return "ashr"; },
        IrOpcode::Lshr => { return "lshr"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper5(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Alloca => { return "alloca"; },
        IrOpcode::Load => { return "load"; },
        IrOpcode::Store => { return "store"; },
        IrOpcode::Gep => { return "gep"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper6(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::Trunc => { return "trunc"; },
        IrOpcode::Zext => { return "zext"; },
        IrOpcode::Sext => { return "sext"; },
        IrOpcode::FpTrunc => { return "fptrunc"; },
        IrOpcode::FpExt => { return "fpext"; },
        IrOpcode::FpToSi => { return "fptosi"; },
        IrOpcode::SiToFp => { return "sitofp"; },
        IrOpcode::FpToUi => { return "fptoui"; },
        IrOpcode::UiToFp => { return "uitofp"; },
        IrOpcode::Bitcast => { return "bitcast"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper7(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::ICmp => { return "icmp"; },
        IrOpcode::FCmp => { return "fcmp"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper8(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::CtPop => { return "ctpop"; },
        IrOpcode::CtLz => { return "ctlz"; },
        IrOpcode::CtTz => { return "cttz"; },
        IrOpcode::Rotl => { return "rotl"; },
        IrOpcode::Rotr => { return "rotr"; },
        IrOpcode::Bswap => { return "bswap"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper9(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::SMin => { return "smin"; },
        IrOpcode::SMax => { return "smax"; },
        IrOpcode::UMin => { return "umin"; },
        IrOpcode::UMax => { return "umax"; },
        IrOpcode::Abs => { return "abs"; },
        IrOpcode::FAdd => { return "fadd"; },
        IrOpcode::FSub => { return "fsub"; },
        IrOpcode::FMul => { return "fmul"; },
        IrOpcode::FDiv => { return "fdiv"; },
        IrOpcode::Fma => { return "fma"; },
        IrOpcode::FAbs => { return "fabs"; },
        IrOpcode::FSqrt => { return "fsqrt"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper10(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::VecAdd => { return "vadd"; },
        IrOpcode::VecSub => { return "vsub"; },
        IrOpcode::VecMul => { return "vmul"; },
        IrOpcode::VecDiv => { return "vdiv"; },
        IrOpcode::VecFAdd => { return "vfadd"; },
        IrOpcode::VecFSub => { return "vfsub"; },
        IrOpcode::VecFMul => { return "vfmul"; },
        IrOpcode::VecFDiv => { return "vfdiv"; },
        IrOpcode::VecMin => { return "vmin"; },
        IrOpcode::VecMax => { return "vmax"; },
        IrOpcode::VecBroadcast => { return "vbroadcast"; },
        IrOpcode::ShuffleVector => { return "shufflevector"; },
        IrOpcode::ExtractElement => { return "extractelement"; },
        IrOpcode::InsertElement => { return "insertelement"; },
        IrOpcode::InlineAsm => { return "inlineasm"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper11(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::AtomicLoad => { return "atomic_load"; },
        IrOpcode::AtomicStore => { return "atomic_store"; },
        IrOpcode::AtomicRmw => { return "atomic_rmw"; },
        IrOpcode::AtomicCmpXchg => { return "atomic_cmpxchg"; },
        IrOpcode::Fence => { return "fence"; },

        else => => { return ""; }
    }
}

static str irOpcodeStrHelper12(IrOpcode op)
{
    switch (op)
    {
        IrOpcode::LandingPad => { return "landingpad"; },
        IrOpcode::ExtractValue => { return "extractvalue"; },
        IrOpcode::GcRoot => { return "gc_root"; },
        IrOpcode::GcWriteBarrier => { return "gc_writebarrier"; },
        IrOpcode::GcSafepoint => { return "gc_safepoint"; },
        IrOpcode::Call => { return "call"; },
        IrOpcode::Phi => { return "phi"; },
        IrOpcode::Select => { return "select"; },

        else => => { return ""; }
    }
}

pub str irOpcodeStr(IrOpcode op)
{
    str r;
    r = irOpcodeStrHelper(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper2(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper3(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper4(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper5(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper6(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper7(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper8(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper9(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper10(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper11(op); if (!r.isEmpty()) return r; },
    r = irOpcodeStrHelper12(op); if (!r.isEmpty()) return r; },
    return "unknown";
}

pub str irICmpStr(IrICmpPred p)
{
    switch (p)
    {
        IrICmpPred::Eq => { return "eq"; },
        IrICmpPred::Ne => { return "ne"; },
        IrICmpPred::Slt => { return "slt"; },
        IrICmpPred::Sle => { return "sle"; },
        IrICmpPred::Sgt => { return "sgt"; },
        IrICmpPred::Sge => { return "sge"; },
        IrICmpPred::Ult => { return "ult"; },
        IrICmpPred::Ule => { return "ule"; },
        IrICmpPred::Ugt => { return "ugt"; },
        IrICmpPred::Uge => { return "uge"; },

        else =>  { return "unknown"; }
    }
}

pub str irAtomicRmwOpStr(IrAtomicRmwOp op)
{
    switch (op)
    {
        IrAtomicRmwOp::Xchg => { return "xchg"; },
        IrAtomicRmwOp::Add => { return "add"; },
        IrAtomicRmwOp::Sub => { return "sub"; },
        IrAtomicRmwOp::And => { return "and"; },
        IrAtomicRmwOp::Nand => { return "nand"; },
        IrAtomicRmwOp::Or => { return "or"; },
        IrAtomicRmwOp::Xor => { return "xor"; },
        IrAtomicRmwOp::Max => { return "max"; },
        IrAtomicRmwOp::Min => { return "min"; },
        IrAtomicRmwOp::UMax => { return "umax"; },
        IrAtomicRmwOp::UMin => { return "umin"; },

        else => { return "unknown" }
    }
}

pub str irAtomicOrderingStr(IrAtomicOrdering o)
{
    switch (o)
    {
        IrAtomicOrdering::Relaxed => { return "relaxed"; },
        IrAtomicOrdering::Acquire => { return "acquire"; },
        IrAtomicOrdering::Release => { return "release"; },
        IrAtomicOrdering::AcqRel => { return "acq_rel"; },
        IrAtomicOrdering::SeqCst => { return "seq_cst"; },

        else => { return unknown" }
    }
}

pub str irFCmpStr(IrFCmpPred p)
{
    switch (p)
    {
        IrFCmpPred::Oeq => { return "oeq"; },
        IrFCmpPred::One => { return "one"; },
        IrFCmpPred::Olt => { return "olt"; },
        IrFCmpPred::Ole => { return "ole"; },
        IrFCmpPred::Ogt => { return "ogt"; },
        IrFCmpPred::Oge => { return "oge"; },
        IrFCmpPred::Ueq => { return "ueq"; },
        IrFCmpPred::Une => { return "une"; },
        IrFCmpPred::Ult => { return "ult"; },
        IrFCmpPred::Ule => { return "ule"; },
        IrFCmpPred::Ugt => { return "ugt"; },
        IrFCmpPred::Uge => { return "uge"; },

        else => { return "unknown"; }
    }

}