// for types

// packed, reorder, nonull, and the rest that were mentioned,
// where compatible, there can be more than one attributed, say,
// [[reorder, packed]]

// more attributes needed. 

// there will also be for funtions
// inline, vectorise.., inline_always, hot, cold...
// can sleo be chained where compatible.

// vectorise can be attributed to single loops a lone too.

// then for switches, fallthrough, which will allow compilation withoout a default else case
// but will still issue a warning
// 
// there will also be more